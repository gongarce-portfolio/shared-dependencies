version: 2
jobs:
  deploy:
    steps:
      - checkout
      - run:
          name: Deploy
          command: |
            cat importmap.json
            echo "Updating import map"
            surge --project ./src
workflows:
  version: 2
  build_and_deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: master